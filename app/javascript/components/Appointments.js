import React from "react"
import PropTypes from "prop-types"
class Appointments extends React.Component {
    render () {
        return (
            <React.Fragment>
            <h1>Appointments</h1>
    </React.Fragment>
    );
    }
}

Appointmnts.propTypes = {
    greeting: PropTypes.string
};
export default Appointments
